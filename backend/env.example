# QIC Backend Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================================================
# SUPABASE CONFIGURATION
# ============================================================================
SUPABASE_URL=your_supabase_url_here
SUPABASE_SERVICE_KEY=your_supabase_service_key_here
USE_SUPABASE=true

# ============================================================================
# AI SERVICES (OpenAI GPT-5 Nano)
# ============================================================================
AI_PROVIDER=openai
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-5-nano
OPENAI_MAX_TOKENS=1000
OPENAI_TEMPERATURE=0.7

# Temporary disable flag - set to 'true' if credits exceeded or API issues
# When credits are restored, set back to 'false' or remove this line
DISABLE_AI_API=false
DISABLE_OPENAI=false

# ============================================================================
# AUTHENTICATION & SECURITY
# ============================================================================
JWT_SECRET=your_jwt_secret_32_chars_minimum_here
ENCRYPTION_KEY=your_32_byte_encryption_key_here

# Clerk Authentication (optional - session-based auth used if not set)
# Get your secret key from https://dashboard.clerk.com
# If not set, app uses session-based authentication (development mode)
CLERK_SECRET_KEY=sk_test_...

# ============================================================================
# CORS CONFIGURATION
# ============================================================================
CORS_ORIGIN=http://localhost:8080,http://localhost:5173

# ============================================================================
# SERVER CONFIGURATION
# ============================================================================
# Server port (default: 3001)
# Backend binds deterministically to this port; exits if in use
PORT=3001

# Environment mode
# - development: relaxed rate limits, verbose logging, offline fallbacks
# - production: strict rate limits, minimal logging
NODE_ENV=development

# ============================================================================
# LOGGING
# ============================================================================
LOG_LEVEL=info

# ============================================================================
# EXTERNAL SERVICES (Optional)
# ============================================================================
# Legacy Lovable.dev (fallback)
# LOVABLE_API_KEY=your_lovable_api_key_here

# ============================================================================
# PRODUCTION NOTES
# ============================================================================
# For production deployment:
# 1. Set NODE_ENV=production
# 2. Use strong, unique JWT_SECRET and ENCRYPTION_KEY
# 3. Update CORS_ORIGIN to your production domains (e.g., https://app.yourdomain.com)
# 4. Set LOG_LEVEL=warn or error for production
# 5. Ensure all external service keys are properly configured
# 6. Set PORT to your production port (or use default 3001)
# 7. Never commit .env to version control
#
# Deployment platforms (Render, Railway, etc.):
# - PORT is usually auto-set by platform
# - Set CORS_ORIGIN to your frontend domain
# - Ensure environment variables are set in platform dashboard